# SESSION_CONTEXT v2 — 2026-08-29

## 1. Contexto Actual y Logros
- **Headroom Proxy + MCP:**
  - Solucionado error inicial de initialización EOF (`headroom mcp install` configuró `headroom mcp serve`).
  - Instaladas dependencias completas (`headroom-ai[proxy]`, `onnxruntime 1.29`, `zstandard`).
  - Auto-arranque de Windows configurado en Startup (`headroom_proxy.vbs` silencioso).
  - Claude Code enrutado de forma permanente vía `~/.claude/settings.json` y `settings.local.json`.
  - Verificado ahorro del 38.5% (32.820 tokens ahorrados en tiempo real).
- **Qwen 3.8 27B Local (Hermes / RTX 4090):**
  - Flags de inferencia optimizados en `qwen_proxy.py`: activado Flash Attention (`-fa`), batch aumentado a 4096 / ubatch 2048.
  - KV Cache optimizado a `q4_0` (`-ctk q4_0 -ctv q4_0`): liberados ~2.3 GB de VRAM (de 21.5 GB a ~19.2 GB en GPU) manteniendo fidelidad intacta.
  - Inactividad reducida de 15 minutos (900s) a 2 minutos (120s) en `moe_proxy.vbs` y `qwen_proxy.py`.
  - Benchmark de estrés sostenido de 1 minuto completado: 2.048 tokens generados a **32.23 tokens/segundo** constantes en CUDA.
  - Decisiones registradas y commiteadas en Obsidian Vault (`decisions/2026-08-29-optimizacion-qwen-local-flash-attention.md`).

- **NotebookLM Master Token Headless Auth & RAG Ecosistema:**
  - Solucionado error recurrente de expiración de sesión (`ValueError: Authentication expired or invalid`).
  - Bootstrap interactivo completado con Chrome real: Master Token persistido en `~/.notebooklm/profiles/default/master_token.json`.
  - Cuaderno `JF.DroneVision` potenciado con 12 fuentes maestras (diseño, contratos, storyboards de Reels 9:16, radar de competencia y ficha técnica AESA).
  - Creada la Guía Maestra de Prompting y Reducción de Tokens (-80%) en el Vault Obsidian (`global/2026-08-29-guia-maestra-prompting-y-reduccion-tokens.md`).
  - Sincronizada la Regla 11 en `.agents/AGENTS.md` para forzar RAG de coste cero vía NotebookLM y serialización densa (TSV/TOON).

## 2. Historial de Decisiones y Estado
- **Claude Code:** Conectado a Headroom proxy (puerto 8787).
- **Hermes:** Mantiene conexión directa a providers oficiales (`config.yaml`) por estabilidad multi-upstream.
- **Antigravity:** Mantiene stack nativo (CBM, RTK, lecturas quirúrgicas).
- **NotebookLM MCP:** Operativo con auto-renovación silenciosa vía Master Token.
- **Regla estricta:** Cada vez que Antigravity invoque modelos locales, matará `llama-server` inmediatamente al finalizar la tarea.

## 3. Archivos Tocados
- `~/.claude/settings.json`
- `~/.claude/settings.local.json`
- `~/.local/bin/cld.cmd`, `cld.ps1`, `claude-h.cmd`, `claude-h.ps1`
- `Startup/headroom_proxy.vbs`
- `Startup/moe_proxy.vbs`
- `~/.hermes/profiles/charla/scripts/qwen_proxy.py`
- `~/.notebooklm/profiles/default/master_token.json`
- `knowledge/decisions/2026-08-29-optimizacion-qwen-local-flash-attention.md`
- `knowledge/decisions/2026-08-29-notebooklm-master-token-headless-auth.md`
- `knowledge/index.md`
