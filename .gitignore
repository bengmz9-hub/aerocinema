node_modules
.next
next-env.d.ts

# ── SECRETOS: NUNCA SUBIR ──
# Cualquier .env (local, produccion, etc) debe quedarse local
.env
.env.*

# ── EXCEPCIÓN: plantilla pública ──
# .env.example es la única que SÍ debe subirse al repo
!.env.example

# ── ARCHIVOS LOCALES ──
*.local

graphify-out/
.vercel
temp_*
*.tmp

# ── CACHÉ DE COMPILACIÓN ──
tsconfig.tsbuildinfo
__pycache__/
*.pyc

# ── WORKTREES LOCALES ──
.worktrees/

# ── DUMPS DE AUDITORÍA (el informe .md SÍ se sube) ──
lighthouse-*.json
